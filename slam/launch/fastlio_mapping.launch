<launch>
  <!-- FAST-LIO2 mapping for the Mid-360 LiDAR (external package fast_lio, not
       modified). Subscribes to /livox/lidar and /livox/imu (published by
       g1_slam's sensors.launch) and publishes /Odometry, /cloud_registered
       and /path. Run sensors.launch separately to bring up the driver. -->

  <rosparam command="load" file="$(find g1_slam)/config/fastlio.yaml"/>

  <!-- Same processing overrides as fast_lio's own mapping_mid360.launch. -->
  <param name="feature_extract_enable" type="bool" value="0"/>
  <param name="point_filter_num" type="int" value="3"/>
  <param name="max_iteration" type="int" value="3"/>
  <param name="filter_size_surf" type="double" value="0.5"/>
  <param name="filter_size_map" type="double" value="0.5"/>
  <param name="cube_side_length" type="double" value="1000"/>
  <param name="runtime_pos_log_enable" type="bool" value="0"/>

  <node pkg="fast_lio" type="fastlio_mapping" name="laserMapping" output="screen"/>
</launch>
